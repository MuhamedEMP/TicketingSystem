version: '3.9'

services:
  backend:
    build:
      context: .
      dockerfile: backend.Dockerfile
    ports:
      - "5172:5172"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Host=postgres;Database=ticketingdb;Username=postgres;Password=postgres
      - ConnectionStrings__RedisConnection=redis:6379
      - ASPNETCORE_URLS=http://+:5172
      - SEED_ADMIN_USERID=mqNr3RairkbRu-6IzRG66hDYJe97qAIofhD7CG3Kvo0
      - SEED_IN_DOCKER=true
      - SEED_ADMIN_EMAIL=bakir.mehremic@emediapatch.com
      - SEED_ADMIN_FIRSTNAME=Bakir
      - SEED_ADMIN_LASTNAME=Mehremic
    depends_on:
      - postgres
      - redis
      

  frontend:
    build:
      context: .
      dockerfile: frontend.Dockerfile
    ports:
      - "5173:5173"
    environment:
    - VITE_API_BASE_URL=http://localhost:5172
    depends_on:
      - backend

  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ticketingdb
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"

volumes:
  postgres_data:
